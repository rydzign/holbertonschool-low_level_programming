#include "holberton.h"

/**
 * main - Entry point
 * function that computes absolute value
 * Return: 0 for (Succes)
 */

int _abs(int n);
{
if (n > 0)
{
return (n);
}
else
{
n = (n * -1);
return (n);
}
}
