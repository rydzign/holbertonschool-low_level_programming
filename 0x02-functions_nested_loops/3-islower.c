#include <holberton.h>

/**
 * main - Entry point
 * program to check for lowercase character.
 * Return: 1 if c is lowercase
 * otherwise Return 0
 */

int _islower(int c);
{
char c;
{
  if (c >= 'a' && c <= 'z')
{
return (1);
}
return (0);
}
