#include "holberton.h"

/**
 *
 *
 *
 */

char *_strcat(char *dest, char *src)
{
	int a;
	int b;

	for (a = 0 ; dest[a] != '\0' ; a++)
		_strcat(*(dest + src)

	{
			(b = 0 ; src[b] != '\0' ; b++)
		}
	return(dest)
}
