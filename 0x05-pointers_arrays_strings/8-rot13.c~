#include "holberton.h"

/**
 *
 *
 */

char *rot13(char *)
{
	int i, j;

	while( j = i )
{
		do(( j = '0'; j >= 'N'; j <= 'M' ) && ( j = '0'; j >= 'n'; j <= 'm' ));
		{

	for(( i = '0'; i >= 'A'; i <= 'M' ) && ( i = '0'; i >= 'a'; i <= 'm' ));
	{
		if( i == i ){
			return(int j);
	}
}
